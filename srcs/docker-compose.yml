version : '3.8'

services :
  nginx :
    container_name : nginx
    build : requirements/nginx
    ports :
      - "443:443"
    networks :
      - inception_net
    #restart : always
  

networks :
  inception_net :
    driver : bridge
